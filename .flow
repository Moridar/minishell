For prompt part
1. Prompt
	For each line
		Split by pipe and send to pipex
2. Pipex
	For each command
		Fork and Execute
3. Execute
    -SetDirection
    -Make_Args (split_shell_cmd
    -Check for built in cmd
    -Get_Path
    -Execve
4. Set direction
	Cut filename and direction sign from cmd
		Interpret the filename
		Eventual open the file
		Returns fd (both in and out)
5. Split_shell_cmd
	Split the cmd
	Interpret the arguments
6. Check for built in
	If the cmd exists as built in
	Run the built in and exit
7. Get_Path
	Get path for the cmd and execve